import { describe, it, expect } from '@jest/globals';

describe('game-rules', () => {
    describe('isMoveValid', () => {
        it('test', () => {
            expect(2+2).toBe(4);
        });
    });
});
